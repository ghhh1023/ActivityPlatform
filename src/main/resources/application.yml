server:
  tomcat:
    uri-encoding: UTF-8
  port: 8080
  servlet:
    context-path: /activityplatform
spring:
  datasource:
    username: root
    password: Ghulalala123?
    url: jdbc:mysql://127.0.0.1:3306/activity_platform?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    password: 123456
    jedis:
      pool:
        max-active: 8
        max-wait: -1s
        min-idle: 0
        max-idle: 8
    timeout: 5s
  application:
    name: io


mybatis:
  type-aliases-package: com.activityplatform.pojo
  configuration:
    map-underscore-to-camel-case: true

my:
  config:
    exclude-uri:
      - /activityplatform/login
      - /activityplatform/getCode
      - /activityplatform/register
